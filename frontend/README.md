# React + TypeScript + Vite frontend for DevBot.

See the [project README](../README.md) for setup, architecture, and how chat / Confirm / the resource map work.

```bash
npm install
npm run dev
```

Dev server: [http://localhost:5173](http://localhost:5173) (proxies `/api` → backend `:8000`).
